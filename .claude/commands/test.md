Run the full test suite and coverage report:

1. Run `npm test` — all tests must pass
2. Run `npm run test:coverage` — report the coverage summary table
3. Flag any source files (not test files) below 80% line coverage
4. Report total test count and pass/fail status
